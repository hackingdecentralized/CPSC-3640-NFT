# Copy to generator/.env. That file is git-ignored.
#
# The secret salt mixed into every seed. Keep it out of the repository and out of
# the website: anyone who has it can compute every token's traits in advance, and
# could grind wallet addresses for rare results before minting.
#
# Generate one with:  openssl rand -hex 32
NFT_SALT=
