# Optional. Copy to generator/.env, which is git-ignored.
#
# `npm run collection -- --network sepolia` needs a Sepolia RPC endpoint. It reads
# SEPOLIA_RPC_URL from the environment, then from this file, then from the
# repository's .env, which usually has it already. Nothing else is read from either.
SEPOLIA_RPC_URL=
